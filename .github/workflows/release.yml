name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v4
        with:
          go-version: '1.24'

      - name: Run tests
        run: go test ./... -v

  build:
    runs-on: ubuntu-latest
    needs: test

    strategy:
      matrix:
        include:
          - os: linux
            arch: amd64
            name: bitbucket-mcp-linux-amd64
          - os: linux
            arch: arm64
            name: bitbucket-mcp-linux-arm64
          - os: darwin
            arch: amd64
            name: bitbucket-mcp-darwin-amd64
          - os: darwin
            arch: arm64
            name: bitbucket-mcp-darwin-arm64

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v4
        with:
          go-version: '1.24'

      - name: Build ${{ matrix.name }}
        env:
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
        run: go build -o ${{ matrix.name }} ./cmd/bitbucket-mcp/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: ${{ matrix.name }}

  release:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/bitbucket-mcp-linux-amd64/bitbucket-mcp-linux-amd64
            artifacts/bitbucket-mcp-linux-arm64/bitbucket-mcp-linux-arm64
            artifacts/bitbucket-mcp-darwin-amd64/bitbucket-mcp-darwin-amd64
            artifacts/bitbucket-mcp-darwin-arm64/bitbucket-mcp-darwin-arm64
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
